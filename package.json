{
  "name": "leafva",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && CI=false npm run build",
    "start": "cd frontend && npm start"
  }
}
