{
  "name": "leafva",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && CI=false npm run build",
    "start": "cd frontend && npm start"
  }
}
